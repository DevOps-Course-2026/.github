name: Lint Markdown

on:
  push:
    branches:
      - main
    paths:
      - '**/*.md'
  pull_request:
    paths:
      - '**/*.md'
  workflow_dispatch:

jobs:
  lint:
    uses: DevOps-Course-2026/.github/.github/workflows/reusable-lint-docs.yml@main
